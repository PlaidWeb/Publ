<?xml version="1.0" encoding="utf-8"?>

<feed xmlns="http://www.w3.org/2005/Atom">

    <title>Publ</title>
    <subtitle>A personal publishing system for the modern web</subtitle>
    <link href="{{ url_for('category',category=category,template='feed',_external=True)}}" rel="self" />
    <link href="{{ url_for('category',category=category,_external=True)}}" />
    <id>tag:{{request.host}},2018-04-01:/{{category.path}}</id>
    <updated>{{view.last_modified.isoformat()}}</updated>

    {% for entry in view.where(recurse=True).entries %}
    <entry>
        <title>{{entry.title}}</title>
        <link href="{{url_for('entry',entry_id=entry.id,_external=_true)}}" rel="alternate" type="text/html" />
        <updated>{{entry.last_modified.isoformat()}}</updated>
        <id>urn:uuid:{{entry.uuid}}</id>
        <content type="html">
            {{entry.body}}
            {% if entry.more %}
            &lt;a href="{{entry.link}}"&gt;Read more...&lt;/a&gt;
            {% endif %}
        </content>
        <author>
            <name>{{entry.author or "fluffy"}}</name>
        </author>
    </entry>
    {% endfor %}
</feed>
